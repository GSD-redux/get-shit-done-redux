{
  "$comment": "Growth ack (#2914 fragment). Reason: #3659 threads the resolved dispatch isolation mode through every worktree.base-check invocation (--mode \"$ISOLATION\") so worktree.baseRef:\"head\" no longer suppresses the divergence comparison in harness-managed mode (the harness does not honor the setting — #48, upstream claude-code#44965); the suppress survives only for orchestrator-managed worktrees where GSD controls the fork start-point. Six invocation lines across three files, one prose update in execute-plan.md; deltas are the flag literal only. Pinned by the #3659 rows in tests/worktree-base-ref.test.cjs.",
  "version": 1,
  "paths": {
    "quick.md": "gsd-core/workflows/quick.md +38B net: --mode \"$ISOLATION\" on both #1941 base-check calls (#3659)",
    "diagnose-issues.md": "gsd-core/workflows/diagnose-issues.md +38B net: --mode \"$ISOLATION\" on both #2649 base-check calls (#3659)",
    "executor-isolation-dispatch.md": "gsd-core/workflows/execute-phase/steps/executor-isolation-dispatch.md +38B net: --mode \"$ISOLATION\" on both base-check calls (#3659)",
    "execute-plan.md": "gsd-core/workflows/execute-plan.md prose update: the #2649 pre-dispatch base-check now threads --mode (#3659)"
  }
}
