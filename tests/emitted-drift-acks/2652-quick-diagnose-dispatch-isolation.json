{
  "version": 1,
  "paths": {
    "quick.md": "#2652 — same migration. Growth is minimized deliberately: the resolution block lives in gsd-core/references/dispatch-isolation-gate.md and this file reads it, because quick.md is at its prompt-stuffing size limit (SIZE_ONLY_WORKFLOWS on next). Net +342 bytes is the reference pointer, the ISOLATION=none degrade pairing, and the post-dispatch bookkeeping conditions re-keyed from the Claude-rendered literal onto ISOLATION (review Blocker).",
    "diagnose-issues.md": "#2652 — migrates the dispatch site off the pre-#2584 RUNTIME != \"claude\" gate onto the negotiated dispatch.isolation seam. Growth is the ISOLATION resolution, the orchestrator-worktree degrade, and the ISOLATION-conditional WORKTREE_GUARD/{harnessFlag} substitution instructions this file had no equivalent of.",
    "execute-plan.md": "#2652 — the fifth dispatch site, converted after review: Pattern A hardcoded isolation=\"worktree\" (Claude Code's literal) gated only on workflow.use_worktrees, with no capability negotiation. Growth is +795 bytes after the #2649 rebase merged its base-check into this paragraph: the USE_WORKTREES/RUNTIME config reads and the pointer at gsd-core/references/dispatch-isolation-gate.md, the resolution block itself living in that reference rather than inline here.",
    "gsd-core/workflows/execute-phase.md": "#2652 review round 3 — not a content change to this workflow, which is byte-identical in the source tree. The delta is emit-time: _stampNonClaudeRuntimeDefaults no longer rewrites the workflow.use_worktrees read to --default false for a runtime whose negotiated dispatch.isolation is not none, so the emitted line keeps the unstamped true default. It moves for exactly the five hosts that declare worktree support (cursor harness-worktree; codex/opencode/kimi/kimi-code orchestrator-worktree) and is unchanged for every isolation-none runtime. Fixes the review Blocker: the install-time stamp resolved USE_WORKTREES=false before dispatch-isolation was ever consulted, so the gate this PR migrates dispatch onto was still deciding isolation by runtime name one layer down."
  }
}
