{
  "version": 1,
  "paths": {
    "execute-phase.md": "#2930 (epic #1671 Phase 3): pilots the in-file `<!-- gsd:section -->` marker grammar by wrapping the --wave/gap-closure/regression-gate branch sections (partial-wave, gap-closure-artifacts, regression-gate) in marker pairs, proving the composeWorkflow seam runs at install time before per-runtime rewrites. Retargeted from plan-phase.md (chore/2930 review): plan-phase.md sits only 36 B under the ADR-857 Phase-6 PRE_PHASE6 gate (tests/phase6-capstone-conformance.test.cjs) and cannot absorb marker overhead, so the maintainer retargeted the pilot to execute-phase.md, which has 728 B of headroom under its own PRE_PHASE6 cap. SOURCE grows by exactly 275 marker bytes (6 marker lines); the EMITTED artifact composeWorkflow produces at install is byte-identical to the pre-#2930 file (markers are stripped, never shipped). See .gsd/phase/chore-2930-fragmentize-xl-workflow/40-design.md 'Known limits' item 5. #2639: handle_branching now warns when local is ahead of origin (+376 B condensed one-line WARNING + rev-list --count check). #2932 (epic #1671 Phase 5): the `initialize` step's field-parse line gains a `section_manifest` mention plus a one-paragraph consumption rule, and each of the three marker bodies (partial-wave, gap-closure-artifacts, regression-gate) gains an `If section_manifest is null or \"<id>\" is in its included list: ... Otherwise skip` guard so the orchestrator reads a step file only when the generated manifest says to (or reads all three, the safe superset, when section_manifest degrades to null). SOURCE grows by +807 B (88700 -> 89507), still 8797 B under the XL hard cap. The EMITTED artifact is NOT byte-identical this time — this growth is genuinely loaded context, not stripped marker overhead — but it is the shrink-enabling change: it is what lets a lazy-file-reading host skip physically reading the excluded steps/*.md bytes, which #2930's marker-only pilot could not yet do."
  }
}
