{
  "version": 1,
  "paths": {
    "execute-phase.md": "#3370: the step-3 executor dispatch gained a Checkpoint gate semantics paragraph (blocking = the default, auto-approvable gate per agents/gsd-executor.md's own <checkpoint_protocol>; only blocking-human always surfaces; the orchestrator must not compose dispatch text refusing or overriding auto-approval) and the Agent() prompt's <objective> gained the matching one-line gate rule, so the dispatched prompt is self-defending in all three isolation modes (harness-worktree verbatim, orchestrator-worktree keeps <objective> verbatim, sequential reuses the structure). Previously the template mentioned no gate semantics at all, so the orchestrator could inject anti-auto-approval text that stalled autonomous runs at gate=\"blocking\" checkpoints. Growth (~630 bytes, 93386 -> 94233, still under the XL 96 KiB ceiling) is entirely the two rule statements — the full gate table stays in the embedded checkpoints.md, not duplicated here. Supersedes the spent #3324 fragment (merged into next), which also named execute-phase.md and would otherwise double-ack the same path.",
    "execute-plan.md": "#3370: the Pattern A dispatch prompt spec gained the same gate-semantics clause (blocking auto-approvable in auto-mode per the executor's own checkpoint protocol; blocking-human always surfaces; add no instruction overriding that protocol), closing the identically-shaped dispatch-time gap on the single-plan path named in the issue. Growth ~248 bytes (38913 -> 39161, still under the DEFAULT 40 KiB ceiling). Supersedes the spent #2652 fragment (merged into next), which also named execute-plan.md and would otherwise double-ack the same path."
  }
}
