{
  "version": 1,
  "paths": {
    "plan-phase.md": "#2650: plan-phase.md grew 13 bytes (94483 -> 94496) net. The stall-detection fix for #2650 moved the bulk of the new logic (gsd_stall_should_recover, gsd_stall_watch, and their explanatory prose) into the lazily-loaded gsd-core/workflows/plan-phase/steps/stall-detection-helpers.md, which is not itself measured against plan-phase.md's frozen ADR-857 Phase 6 PRE_PHASE6 cap (tests/phase6-capstone-conformance.test.cjs). That extraction alone left plan-phase.md 142 bytes SMALLER than baseline. Verification then surfaced two more consequences: (1) tests/plan-phase-drift-guard.test.cjs (#913) requires at least 7 literal \"ORCHESTRATOR RULE — ALL RUNTIMES\" labels in plan-phase.md itself, one per agent spawn site — the extraction had carried 5 of the 7 labels out with the blocks they lived in, so a short label was restored at each of the 5 stall-watch spawn sites, leaving the explanatory bodies in the extracted reference file; (2) an independent review found PLANNER_OUTPUT_FILE/CHECKER_OUTPUT_FILE were referenced but never assigned anywhere (a dead variable — marker_received was permanently unreachable, and the plan-checker spawn specifically has no other completion signal on a clean PASS since a passing checker touches no *-PLAN.md files), fixed by replacing the dead bash variable with the {outputFile} orchestrator-substitution token already used by docs-update.md:471, which is a net BYTE SAVINGS at each site (`\"{outputFile}\"` is shorter than `\"$PLANNER_OUTPUT_FILE\"`) — the pointer sentence in step 7.99 noting where {outputFile} is documented is the actual source of the remaining growth. Net effect of both fixes: +13 bytes over baseline, verified still under the PRE_PHASE6 cap (94496 < 94519, 22 bytes headroom) and the #913 label-count requirement (7 present)."
  }
}
