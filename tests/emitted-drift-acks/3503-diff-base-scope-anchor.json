{
  "version": 1,
  "paths": {
    "gsd-core/workflows/code-review.md": "#3503: +915 bytes. All three diff-base derivations in this file (Tier-3 file-scope fallback; spawn_reviewer's agent-context DIFF_BASE) replace the #2989/#3191 prose anchor — git log --grep=\"[Pp]hase ${PADDED_PHASE}([^[:alnum:]_]|$)\" — with a subject-line conventional-commit scope anchor (--grep=\"^[[:alpha:]]+!?\\((phase-)?(${PHASE_SCOPE_NUM})(-[0-9]+)?\\)!?:\" under --extended-regexp). The prose phrase matched free prose in ANY commit body (planning commits forward-reference later phases: 'deferred to Phase N per D-09'; doc commits use '### Phase N' as a format example) while GSD's own commits — which use scopes like docs(phase-6):, feat(6-01):, docs(6): — never contain the literal 'Phase N'; with tail -1 keeping the OLDEST match, each prose false positive resolved the base unboundedly before the phase (#3503: ~4 phases early on a real repo, silently inflating the #2666 SUMMARY/diff union scope). PHASE_SCOPE_NUM derives the padded/unpadded alternation from PADDED_PHASE because workflows emit the UNPADDED roadmap number while code-review greps the zero-padded one. POSIX-ERE portability (#3191, no \\b) and the fail-closed empty-result warning are preserved.",
    "gsd-core/workflows/code-review/steps/structural-pre-pass.md": "#3503: +325 bytes. The fallow --changed-since base derivation carries the identical scope-anchored conventional-commit grep (plus the same PHASE_SCOPE_NUM padded/unpadded prep) in lockstep with the two code-review.md sites, per the #3191 same-derivation contract — the prose anchor it replaced captured 'Phase N' mentions in arbitrary commit bodies and silently widened the structural pre-pass far past the phase.",
    "code-review.md": "#3503: +915 bytes vs next. The emitted workflow copy grows with its source gsd-core/workflows/code-review.md — the scope-anchored diff-base grep, the PHASE_SCOPE_NUM padded/unpadded prep, and the #3503 contract comments at both derivation sites (Tier-3 fallback and spawn_reviewer). Replaces the spent 3191-unanchored-grep-sites.json fragment (its code-review.md entry was consumed when #3191 merged and could no longer clear anything)."
  }
}
