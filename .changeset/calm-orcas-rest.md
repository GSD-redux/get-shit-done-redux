---
type: Fixed
pr: 0
---
**`config-get --default` now emits the JSON value instead of a double-encoded JSON string** — `/gsd-code-review` no longer hard-stops before agent spawn in projects that never set `workflow.code_review_depth_overrides`. (#4262)
